<div class="row">
  <div class="col s8">
    <div class="paper-sheet z-depth-1">
      <%= fa_icon 'lightbulb-o', 'data-id': question.id, class: 'right btn-icon' %>
      <span>Passo <%= question.index %></span>
      <h2 class="title"><%= question.title %></h2>

      <%= question.content.html_safe %>
    </div>
    <div class="react-keyboard-app"></div>
  </div>


  <div class="col s4">
    <div id="tips-<%= question.id %>" class="paper-sheet z-depth-1 tips">
      <h4 class="title center-align"><%= fa_icon 'lightbulb-o', text: 'Dicas' %></h4>
      <%= render question.tips_to_show(user: current_user, team: @team) %>
    </div>
  </div>
</div>
