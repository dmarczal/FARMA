<% provide(:title, "Turmas matriculadas") %>

<article>
  <header class="center">
    <h1>Turmas matriculadas</h1>
  </header>

  <%= split_in_columns(collection: @teams, per_row: 3) do |team|%>
    <%= render 'team', team: team %>
  <% end %>
</article>
