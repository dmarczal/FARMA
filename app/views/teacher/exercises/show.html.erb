<% provide(:title, "Exercício #{@exercise.title}") %>

<div class="container-fluid">
  <ul class="collapsible">
    <li>
      <div class="collapsible-header center">
        <h2><%= @exercise.title %></h2>
      </div>
      <div class="collapsible-body paper-sheet">
        <%= @exercise.content.html_safe %>
      </div>
    </li>
  </ul>
  <%= link_to teacher_test_lo_path(@lo, page: @page), class: 'waves-effect waves-light btn' do %>
    Testar OA
  <% end %>

  <div id="steps-app" data-exercise-id="<%= @exercise.id %>"></div>
</div>
